pre-commit:
  # parallel: true  # run tasks in parallel
  commands:
    format-python:
      run: |
        black .
      glob: "*.py"
    format-shell:
      run: |
        shfmt -w ./scripts
      glob: "*.sh"
# TODO: port these to scripts and add unit tests
# pre-commit:
#   parallel: true
  # scripts:
  #   format-python:
  #     run: black .
  #     glob: "*.py"
  #   format-shell:
  #     run: shfmt -w ./scripts
  #     glob: "*.sh"
